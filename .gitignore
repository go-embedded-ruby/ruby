# Built CLI binary
/rbgo

# Built wasm playground artifacts (regenerate with web/build.sh)
/web/rbgo.wasm
/web/wasm_exec.js

# Coverage profiles
*.out
cov.out

# Go test binaries (e.g. `go test -c`)
*.test

# Editor / OS noise
.DS_Store
/rbgo.exe

# built binaries
rbgo-bin
